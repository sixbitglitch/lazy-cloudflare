# Example Cloudflare Tunnel config.
# Copy to ~/.cloudflared/config.yml or /etc/cloudflared/config.yml
# and set tunnel to your tunnel UUID (from: cloudflared tunnel list).

# tunnel: <TUNNEL_UUID>
# credentials-file: /path/to/.cloudflared/<TUNNEL_UUID>.json

# ingress:
#   - hostname: app.example.com
#     service: http://localhost:3000
#   - hostname: api.example.com
#     service: http://localhost:8080
#   - service: http_status:404
